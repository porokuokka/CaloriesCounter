<UserControl
    x:Class="CaloriesCounter.MessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CaloriesCounter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="768"
    d:DesignWidth="1366">

    <Grid Background="Transparent">
        <Border BorderBrush="{StaticResource ApplicationForegroundThemeBrush}" CornerRadius="5,5,5,5"  BorderThickness="1" Margin="270,192,270,176" >
            <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
                <TextBlock x:Name="TextBlockMessage" HorizontalAlignment="Left" Margin="75,61,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Style="{StaticResource PageSubheaderTextStyle}" Height="228" Width="659"/>
                <Button x:Name="ButtonOk" Content="Ok" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="387,0,376,26" Click="ButtonOk_Clicked" Width="61"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
